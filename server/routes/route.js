import express from "express";

const router = express.Router();


// router.route('/')
//       .get(getAllBooks)
//       .post(addBook)

// router.route('/:id')
//       .put(updateBook)
//       .delete(deleteBook);

export default router;
     